import styled from 'styled-components';

export const FeaturedBlogsStyles = styled.div`
  padding: 5rem 0;
  
  .featuredBlogs__text {
    max-width: 600px;
    margin: 1.5rem auto 3rem auto; /* Centers the block and pushes grid down */
  }
  
  .centre__text {
    text-align: center;
  }
`;